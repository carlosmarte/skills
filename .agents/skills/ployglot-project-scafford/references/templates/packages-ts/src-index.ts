// Public surface of <scope-prefix><slug>.
//
// This module re-exports only — implementations live in sibling modules
// under `src/`. Keep the surface here in lockstep with `__all__` in the
// Python twin's `<snake_pkg>/__init__.py`.

export const VERSION = '0.1.0';

// export { ... } from './...';
