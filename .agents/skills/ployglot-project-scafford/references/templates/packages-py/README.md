# <slug> (Python)

Python twin of [`packages/ts/`](../ts/). <subject>.

The canonical behavior contract is at the repo root in
[`SPEC.md`](../../SPEC.md). Cross-language parity is enforced by
[`tests/parity/fixtures.json`](../../tests/parity/fixtures.json).

## Install

```bash
pip install <slug>
```

## Usage

See [`packages/py/.agent.md`](.agent.md) for the canonical surface
listing and a usage snippet that mirrors the TypeScript twin.
